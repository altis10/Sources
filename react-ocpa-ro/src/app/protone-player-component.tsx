import React from 'react';

const ProtonPlayerComponent = () => {
    return (
        <a>ProTONE Player have nothing to see for now. Please stay tuned.</a>
    );
};

export default ProtonPlayerComponent;